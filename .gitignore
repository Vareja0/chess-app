# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Compiled Go binaries
go-jwt
main

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment / secrets
.env
.env.*
!.env.example

# Helm chart dependencies and lock
chess-app/charts/
chess-app/Chart.lock

# Kubernetes secrets / override values (production credentials)
*values-prod.yaml
*values-local.yaml
*values.override.yaml

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS artifacts
.DS_Store
Thumbs.db

# Design reference (not part of the codebase)
stitch/

# Terraform state and local data
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.d/
.terraform/
.terraform.lock.hcl
*.tfplan
*.tfvars
!*.tfvars.example